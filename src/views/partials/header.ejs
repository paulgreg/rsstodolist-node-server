<header class="page-header">
    <div class="header-container">
        <h1>
            <a href="<%= rootUrl %>" class="title"
                ><%= t('common.site_title') %></a
            >
        </h1>
        <%- include('language-selector') %>
    </div>
    <p class="lead"><%= description %></p>
</header>
